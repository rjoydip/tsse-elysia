info:
  name: Get Account
  type: http
  seq: 2
  tags:
    - settings
    - smoke

http:
  method: GET
  url: "{{base_url}}/settings/account"
  auth:
    type: bearer
    token: "{{session_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true