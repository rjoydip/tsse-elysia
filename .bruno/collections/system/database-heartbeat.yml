info:
  name: Database Heartbeat
  type: http
  seq: 2
  tags:
    - system
    - smoke

http:
  method: GET
  url: "{{base_url}}/database/heartbeat"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true