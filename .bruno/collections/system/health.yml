info:
  name: Health
  type: http
  seq: 1
  tags:
    - smoke
    - system

http:
  method: GET
  url: "{{base_url}}/health"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true