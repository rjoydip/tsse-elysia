info:
  name: Email Render - Invalid
  type: http
  seq: 2
  tags:
    - email

http:
  method: POST
  url: "{{base_url}}/email/render"
  headers:
    - name: Content-Type
      value: application/json
  body: json
  json: |-
    {
      "template": ""
    }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 400", () => expect(res.status).to.equal(400));

settings:
  encodeUrl: true