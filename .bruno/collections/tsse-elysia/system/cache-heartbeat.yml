info:
  name: Cache Heartbeat
  type: http
  seq: 3
  tags:
    - system
    - smoke

http:
  method: GET
  url: "{{base_url}}/cache/heartbeat"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true