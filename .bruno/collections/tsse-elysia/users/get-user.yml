info:
  name: Get User by ID
  type: http
  seq: 3
  tags:
    - users

http:
  method: GET
  url: "{{base_url}}/users/{{user_id}}"
  auth:
    type: bearer
    token: "{{session_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true