info:
  name: My Permissions
  type: http
  seq: 2
  tags:
    - smoke
    - roles

http:
  method: GET
  url: "{{base_url}}/roles/permissions/mine"
  auth:
    type: bearer
    token: "{{session_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is 200", () => expect(res.status).to.equal(200));

settings:
  encodeUrl: true